\newcommand{\conteoTotal}{%
  \immediate\write18{texcount -1 -sum -merge content/*.tex | sed 's/ (errors:[0-9]*)//g' > palabras.sum}%
  \IfFileExists{palabras.sum}{%
    \input{palabras.sum} words%
  }{%
    ?? words (habilitar shell-escape)%
  }%
}